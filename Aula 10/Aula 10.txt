! (exclamação)+ TAB dá as configurações iniciais do html

O Css é manipulado pelo html (solicita estilização), por isso vem antes da página html

Java script manipula os dados do html, por isso será inserido depois da página no body.

Console.log = Ele esceve uma mensagem no console do java script
// ...... - Comentario em Java script

let "variavel" - Está declarando uma variavel na java script. 
Obs:A variavel não pode ter espaço e a 2°palavra tem que ser inciada por letra maiuscula.
${variavel} é a forma de resgatar uma variavel no let para o console java script
If é um valor de variavel e o else é o valor contrario a variavel do If

"" somente para texto
= está retomando o valor indicado
== é o igual(comparando) ignorando o tipo da informação (número, texto, etc.)
=== é o igual(comparando) exatamente
! - é o valor contrario da sentença.
+ para texto concatena e para números soma
